---
openapi: 3.0.3
info:
  title: dev-games-challenge-5 API
  version: 1.0.0-SNAPSHOT
paths:
  /goodbye:
    get:
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
      security:
        - api-key: []
  /goodbye/{name}:
    get:
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
      security:
        - api-key: []
  /hello:
    get:
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
      security:
        - api-key: []
  /hello/{name}:
    get:
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
      security:
        - api-key: []
components:
  securitySchemes:
    api_key:
      type: apiKey
      name: api_key
      in: header